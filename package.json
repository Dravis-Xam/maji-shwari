{
  "name": "maji-shwari-deployment",
  "private": true,
  "scripts": {
    "build": "npm install --prefix app && npm run build --prefix app",
    "dev": "npm run dev --prefix app"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.2"
  }
}
